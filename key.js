const computer = {
  brand: "HP",
  price: 30000,
  processor: "intel",
  ssd: 512,
};

const keys = Object.keys(computer);
console.log(keys);

const values = Object.values(computer);
console.log(values);


